{
  "name": "woffu-signer",
  "version": "1.0.0",
  "description": "",
  "author": "Miguel Cruz",
  "license": "ISC",
  "main": "src/app.ts",
  "scripts": {
    "start": "tsc && node -r dotenv/config dist/app.js",
    "debug": "tsc && node -r dotenv/config --nolazy --inspect-brk=9229 dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/node": "^14.0.5",
    "axios": "^0.19.2",
    "dotenv": "^8.2.0",
    "typescript": "^3.9.3"
  },
  "devDependencies": {
    "tslint": "^5.12.1"
  }
}
